spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:63306/book_eye
    username: root
    password: test

app:
  api:
    password:
      bcrypt-strength: 10
  gateway:
    env:
      system-setting:
        access-key: integration-test-system-access-key
